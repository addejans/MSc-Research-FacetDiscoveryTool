# Artifacts

This folder contains historical non-source artifacts from the MSc research project.

- `presentations/` — presentation PDFs and slides.
- `archives/` — original zip uploads retained for provenance.

These are preserved so the research history is not lost, but new readers should start with the top-level README and the `docs/` folder.
